<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <!-- Legacy Bluetooth permissions for Android 11 (API 30) and below -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!--
      Location permission for BLE scanning below Android 12 (API 31). Both declarations are
      required because PermissionService.requiredPermissionsForScan() asks for
      ACCESS_FINE_LOCATION on API 29-30 and ACCESS_COARSE_LOCATION on API 24-28, and a
      permission this library never declares is denied outright: requestPermissions returns
      DENIED for it without showing a dialog, so the scan can never start. Keep the
      maxSdkVersion caps aligned with those branches.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30"/>

    <!-- Declare that the app uses Bluetooth LE -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <application>
        <activity
            android:name="jp.yuta.miura.unityble.PermissionRequester"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    </application>

</manifest>